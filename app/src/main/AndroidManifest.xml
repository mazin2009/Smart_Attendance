<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mm_kau.smartattendance">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginPage"
            android:label="Home Page">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".adminHome"
            android:label="Admin Page" />
        <activity android:name=".Manage_Course" />
        <activity android:name=".Teacher_HomePage" />
        <activity android:name=".Student_HomePage" />
        <activity android:name=".Manage_Teacher" />
        <activity android:name=".Manage_Student" />
        <activity android:name=".ManageClassRoom" />
        <activity
            android:name=".Manage_clasroom"
            android:label="@string/title_activity_manage_clasroom"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Manage_classRoom" />
        <activity android:name=".course_info_for_teacher" />
        <activity android:name=".listOfAttenanceInfo_forEachLecture" />
        <activity android:name=".course_Info_for_student"></activity>
    </application>

</manifest>